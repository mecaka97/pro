#include <stdio.h>

int main() {
    int i,pn;
    for (i =1 ; i < 100 ; i++)
    {
        pn =i*(3*i-1)/2;
        
        if((pn < 100)){
            printf("%d\n",pn);
        }
    }
    return 0;
}
